name: text_controller_manager
description: A library to auto-generate TextEditingControllers.
version: 0.0.3

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

homepage: https://github.com/JingSongJai/text_controller_manager

repository: https://github.com/JingSongJai/text_controller_manager

dependencies:
  flutter:
    sdk: flutter
  yaml: ^3.1.3
  logging: ^1.3.0
  path: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

executables:
  generate:

flutter:
